/** @type {import('next').NextConfig} */
const nextConfig = {
  // This tells Next.js to prefix all asset URLs with /app1
  // assetPrefix: '/app1',
  // // Optional: if you're using basePath (recommended for subpath deployments)
  // basePath: '/app1',
};

export default nextConfig;
